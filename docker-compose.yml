version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    container_name: game_postgres
    environment:
      POSTGRES_DB: game_dw
      POSTGRES_USER: game_user
      POSTGRES_PASSWORD: game_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - game_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U game_user -d game_dw"]
      interval: 5s
      timeout: 5s
      retries: 5

  notebook:
    image: jupyter/scipy-notebook:python-3.11
    container_name: game_notebook
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN=epsi2024
    ports:
      - "8888:8888"
    volumes:
      - C:\Users\Papis\Documents\EPSI\MASTER 1\DataMart\notebooks:/home/jovyan/work/notebooks
      - C:\Users\Papis\Documents\EPSI\MASTER 1\DataMart\Data:/home/jovyan/work/data:ro
      - C:\Users\Papis\Documents\EPSI\MASTER 1\DataMart\sql:/home/jovyan/work/sql:ro
      - C:\Users\Papis\Documents\EPSI\MASTER 1\DataMart\diagrams:/home/jovyan/work/diagrams:ro
      - C:\Users\Papis\Documents\EPSI\MASTER 1\DataMart\requirements.txt:/home/jovyan/work/requirements.txt:ro
      - "C:/Users/Administrateur/Desktop/DATAMART/epsi-datamart-lol/V2:/home/jovyan/work"
      - "C:/Users/Administrateur/Desktop/DATAMART/epsi-datamart-lol/data:/home/jovyan/work/data:ro"
      - "C:/Users/Administrateur/Desktop/DATAMART/epsi-datamart-lol/SQL:/home/jovyan/work/sql:ro"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - game_network
    command: >
      bash -c "
        pip install --no-cache-dir pandas sqlalchemy psycopg2-binary matplotlib seaborn ijson &&
        start-notebook.sh --NotebookApp.token='epsi2024'
      "

  postgres_metabase:
    image: postgres:15-alpine
    container_name: metabase_db
    environment:
      POSTGRES_DB: metabase
      POSTGRES_USER: metabase
      POSTGRES_PASSWORD: metabase_password
    volumes:
      - metabase_data:/var/lib/postgresql/data
    networks:
      - game_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U metabase -d metabase"]
      interval: 5s
      timeout: 5s
      retries: 5

  metabase:
    image: metabase/metabase:latest
    container_name: game_metabase
    environment:
      - MB_DB_TYPE=postgres
      - MB_DB_DBNAME=metabase
      - MB_DB_PORT=5432
      - MB_DB_USER=metabase
      - MB_DB_PASS=metabase_password
      - MB_DB_HOST=postgres_metabase
    ports:
      - "3000:3000"
    depends_on:
      postgres_metabase:
        condition: service_healthy
    networks:
      - game_network

  dozzle:
    image: amir20/dozzle:latest
    container_name: game_dozzle
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "9999:8080"
    networks:
      - game_network

volumes:
  postgres_data:
  metabase_data:

networks:
  game_network:
    driver: bridge
